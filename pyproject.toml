[project]
name = "ducklake-tests"
version = "0.1.0"
description = "Benchmark INSERT performance into a DuckLake table over the DuckDB Quack client-server protocol"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.3",
    "faker>=40.23.0",
]
